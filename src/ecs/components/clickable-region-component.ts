export class ClickableRegionComponent {
    public startX: number;
    public startY: number;
    constructor(startX: number, startY: number) {
        this.startX = startX;
        this.startY = startY;
    }
}
