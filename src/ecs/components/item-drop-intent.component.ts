export class ItemDropIntentComponent {
    public whereX: number;
    public whereY: number;
    public offsetY: number;
    constructor(whereX: number, whereY: number, offsetY: number) {
        this.whereX = whereX;
        this.whereY = whereY;
        this.offsetY = offsetY;
    }
}
