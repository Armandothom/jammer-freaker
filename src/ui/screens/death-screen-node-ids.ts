export const DEATH_SCREEN_NODE_IDS = {
  root: "death.overlay.root",
  background: "death.overlay.background",
  deathPrompt: {
    root: "death.prompt.root",
    background: "death.prompt.background",
    text: "death.prompt.text",
  },
  retryButton: {
    root: "death.prompt.retry-button.root",
  },
  quitButton: {
    root: "death.prompt.quit-button.root",
  },
} as const;
